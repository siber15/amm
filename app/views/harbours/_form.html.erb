<%= simple_form_for @harbour, :html => { :class => "form-horizontal harbour" } do |f| %>

  <% if @harbour.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@harbour.errors.count, "error") %> prohibited this harbour from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @harbour.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :zip %>
  <%= f.input :city %>
  <%= f.input :country %>
  <%= f.input :phone %>
  <%= f.input :description %>
  <%= f.input :active %>
  <%= f.submit "Opret Havn", class: "btn btn-success" %>

<% end %>
